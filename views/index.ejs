<% include ./partials/header %>
<p>
  <% blogs.forEach((blog) => { %>
    <div>
      <h2><%= blog.title %></h2>
      <img src='<%= blog.image %>'/>
      <span><%= blog.created %></span>
      <p>
        <%= blog.body %>
      </p>
    </div>
  <% }) %>
</p>
<% include ./partials/footer %>
